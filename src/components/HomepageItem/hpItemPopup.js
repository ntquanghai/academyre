import React from "react";

export function hpPopup(props) {
    const img = props.img
    const courseName = props.courseName
    const desc = props.courseDesc
    const author = props.author
    return(
        <div className = "" style = {{width:"400px",height:"400px"}}></div>
    )
}