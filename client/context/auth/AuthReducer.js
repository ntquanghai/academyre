import AuthAPI from "../../src/services/auth"

import {
  FETCH_USER,
  FETCH_USER_ERROR,
  LOGIN_FAIL,
  LOGIN_REQUEST,
  LOGIN_SUCCESS,
  LOG_OUT,
  SIGNUP_FAIL,
  SIGNUP_REQUEST,
  SIGNUP_SUCCESS,
} from "../types";

const redicer = (state, action) => {
  
}